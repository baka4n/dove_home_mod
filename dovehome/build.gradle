for (final def f in ["ae2", "botania","create","immersive","jei","mek","twilight"]) {
    apply from: rootProject.file("gradle/dependenciesPath/${f}.gradle")
}

dependencies {
    implementation project(":dovedb")
    implementation project(":dovegod")
}
